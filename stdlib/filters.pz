func paintOver()
	blendFunc([:SrcAlpha, :OneMinusSrcAlpha], [:One, :Zero])
	animationStyle(:paintOver)

func motionBlur()
	blendFunc([:SrcAlpha, :OneMinusSrcAlpha], [:One, :Zero])
	animationStyle(:motionBlur)
